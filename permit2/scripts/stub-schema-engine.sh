#!/bin/bash
# Stub schema-engine for offline/sandboxed environments.
# Used when prisma generate is run without network access.
# This stub allows prisma generate to succeed for client generation
# (which does not actually need the schema-engine binary).
exit 0
